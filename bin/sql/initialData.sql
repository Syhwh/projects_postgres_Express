CREATE TABLE users(
user_id BIGINT UNIQUE NOT NULL GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
user_name  TEXT NOT NULL,
user_email VARCHAR (255) NOT NULL,
user_password VARCHAR (255) NOT NULL
);

CREATE TABLE projects(
	project_id BIGINT UNIQUE NOT NULL GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
	project_name CHARACTER VARYING(20) NOT NULL,
	user_id BIGINT REFERENCES users(user_id)
);

CREATE TABLE tasks(
	task_id BIGINT UNIQUE NOT NULL GENERATED ALWAYS AS IDENTITY,
	task_name CHARACTER VARYING(20) NOT NULL,
	tasK_description CHARACTER VARYING(20) NOT NULL,
	task_status BOOLEAN NOT NULL DEFAULT FALSE,
	project_id BIGINT  REFERENCES projects(project_id)
);